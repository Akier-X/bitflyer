# Docker Compose for Local Development
# =====================================
# Usage: docker-compose up -d

version: "3.8"

services:
  trader:
    build: .
    container_name: ultimate-ai-trader
    ports:
      - "8080:8080"
    environment:
      - PAPER_TRADING=true
      - LINE_NOTIFY_TOKEN=${LINE_NOTIFY_TOKEN:-}
      - BITFLYER_API_KEY=${BITFLYER_API_KEY:-}
      - BITFLYER_API_SECRET=${BITFLYER_API_SECRET:-}
      - PORT=8080
    volumes:
      - ./logs:/app/logs
      - ./models:/app/models
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Redis for caching (if needed)
  # redis:
  #   image: redis:alpine
  #   container_name: trader-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

# volumes:
#   redis_data:
